import React from 'react';
import s from './button_add_product.module.css';

export default function ButtonAddProduct() {
   return (
      <button className={s.button} type="button" onClick={() => {}}>
         +
      </button>
   );
}
